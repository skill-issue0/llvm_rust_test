name: LLVM Compilation

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Download LLVM Source Code
        run: |
          wget -O llvm.tar.gz https://github.com/llvm/llvm-project/archive/refs/tags/llvmorg-16.0.0.tar.gz
          tar xzf llvm.tar.gz
          mv llvm-project-llvmorg-16.0.0 llvm-project

      - name: Add CMakeLists.txt to build directory
        run: |
          echo 'cmake_minimum_required(VERSION 3.13.4)' > llvm-project/build/CMakeLists.txt
          echo 'project(MyLLVM)' >> llvm-project/build/CMakeLists.txt
          echo 'add_subdirectory(llvm)' >> llvm-project/build/CMakeLists.txt

      - name: Build LLVM
        run: |
          cd llvm-project
          mkdir build
          cd build
          cmake -GNinja -DLLVM_ENABLE_PROJECTS="llvm;clang" -DCMAKE_BUILD_TYPE=Release ..
          ninja

      - name: Create LLVM Archive
        run: |
          cd llvm-project/build
          ninja package

      - name: Upload LLVM Archive
        uses: actions/upload-artifact@v2
        with:
          name: llvm-artifacts
          path: llvm-project/build/llvm-*.tar.gz
